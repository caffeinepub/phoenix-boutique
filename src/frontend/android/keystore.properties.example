# Android Release Signing Configuration (Example)
# 
# IMPORTANT: This is an example file. DO NOT commit actual passwords or keystores.
# 
# To use release signing:
# 1. Copy this file to keystore.properties (same directory)
# 2. Replace placeholder values with your actual keystore details
# 3. Ensure keystore.properties is in .gitignore (it should be by default)
#
# The keystore.properties file is used by android/app/build.gradle to sign release builds.

# Path to your release keystore file (relative to android/ directory)
# Example: keystores/release.keystore
storeFile=keystores/release.keystore

# Keystore password
# Replace with your actual keystore password
storePassword=YOUR_KEYSTORE_PASSWORD_HERE

# Key alias used when generating the keystore
# Replace with your actual key alias
keyAlias=phoenix-boutique-release

# Key password
# Replace with your actual key password
keyPassword=YOUR_KEY_PASSWORD_HERE

# ============================================================================
# SECURITY NOTES:
# ============================================================================
# - Never commit keystore.properties with real passwords to version control
# - Store passwords securely (password manager, CI/CD secrets, etc.)
# - Keep your keystore file backed up in a secure location
# - If you lose your keystore, you cannot update your app on Play Store
# ============================================================================
